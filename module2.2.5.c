#include <stdio.h>

void main( void )
{
  float num1, num2, sum, mul, sub, cut;
  printf("Input numbers: ");
  scanf(%f %f, &num1, &num2);
  printf("Sum: %f, mul: %f, sub: %f, cut: %f", num1 + num2, num1 * num2, num1 - num2, num1 / num2);
  return(0);
}
